const add = function addFn(num1, num2) { num1 + num2;}

const subtract = function subtractFn(num1, num2) { num1 - num2;}

const multiply = function multiplyFn(num1, num2) { num1 * num2;}

const divide = function addFn(num1, num2) { num1 / num2;}

let firstNum;
let operator;
let secondNum;

const operate = function operate(firstNum, secondNum, operator) {
    // let result = operator(firstNum, secondNum)
}
